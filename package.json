{
  "name": "@philipshue/cli",
  "version": "0.1.0",
  "description": "Agent-native CLI for controlling Philips Hue smart lights (POC, mocked backend).",
  "type": "module",
  "bin": {
    "philips-hue": "bin/philips-hue"
  },
  "files": [
    "bin",
    "dist",
    "skills"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "biome check src/"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^20.14.0",
    "typescript": "^5.5.4",
    "vitest": "^2.1.0"
  }
}
